/** Shared design tokens. Import "@pi-desktop/ui-kit/tokens.css" for the CSS variables. */
export const tokens = {
  colorBg: "var(--pd-bg)",
  colorSurface: "var(--pd-surface)",
  colorBorder: "var(--pd-border)",
  colorText: "var(--pd-text)",
  colorTextDim: "var(--pd-text-dim)",
  colorAccent: "var(--pd-accent)",
  colorDanger: "var(--pd-danger)",
  radius: "var(--pd-radius)",
  font: "var(--pd-font)",
  fontMono: "var(--pd-font-mono)",
} as const;
